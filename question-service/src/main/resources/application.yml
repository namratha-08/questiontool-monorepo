spring:
  datasource:
    url: jdbc:mysql://localhost:3306/question_tool
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # use 'create' temporarily to recreate table
    show-sql: true
  sql:
    init:
      mode: always

server:
  port: 8082
